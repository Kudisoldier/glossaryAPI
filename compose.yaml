services:
  api:
    build: .
    image: glossaryapi:latest
    ports:
      - "8000:8000"
    volumes:
      - ./glossary.db:/app/glossary.db
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
